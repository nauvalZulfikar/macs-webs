# Copy to .env and fill in values
# Push notification token (used by /api/notify/* endpoints)
PW_TOKEN=your-token-here

# Server bind config
BIND_HOST=127.0.0.1
BIND_PORT=8101

# Dev bind = localhost only; Prod bind = Tailscale IP (NOT 0.0.0.0)
DEV_BIND_HOST=127.0.0.1
PROD_BIND_HOST=100.x.x.x
